{
  "name": "@anion155/monorepo",
  "private": true,
  "scripts": {
    "_packages": "pnpm -r --filter='!configs' --filter='!polyfill-base'",
    "lint": "pnpm run _packages run lint",
    "test": "pnpm run _packages run test"
  }
}
